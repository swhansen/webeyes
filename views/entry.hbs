<div class = "centerFlex">
<p id="login-message">Start a New WEG2RT Session</p>
    <form id ="pwform" action="">
        <label>Password</label>
        <input type="password" name="password" value="password"/><br>
        <label>Room Name (optional)</label>
        <input type="text" name="room-name" value="room-name"/><br>
        <label>User ID (optional)</label>
        <input type="text" name="uid" value="uid"/><br><br>

        <div id="entryformbutton">
        <input type="submit" id="entry-submit" value="Go Collaborate"  />
        </div>
        <pre id="result">
    </form>
 </div>
 <div class= "about">
 <p>
 <a href="/about">What is this all about?</a>
 </p>
 </div>
        <script type="text/javascript" src="bower/jquery/dist/jquery.min.js"></script>

    <script>

$.fn.serializeObject = function()
{
    var o = {};
    var a = this.serializeArray();
    $.each(a, function() {
        if (o[this.name] !== undefined) {
            if (!o[this.name].push) {
                o[this.name] = [o[this.name]];
            }
            o[this.name].push(this.value || '');
        } else {
            o[this.name] = this.value || '';
        }
    });
    return o;
};

</script>





    <script>
  //  $( document ).ready( function() {
        $( "#pwform" ).on( "submit",  function( event ) {
            console.log( "password sumbitted" );

//var str = $( "pwform" ).serialize();

$('#result').text(JSON.stringify($('pwform').serializeObject()));


            var pw = {
                "pw": $( "input" ).val()
            };

            $.post( "/", pw, function( response, status ) {
                console.log( "PW:" + JSON.stringify( pw.pw ) + "\nstatus:" + status );
                if ( response == "logged in" ) {
                    location.reload();
                } else {
                    $( "body" ).text( response );
                }
                console.log( JSON.stringify( response ) );
            } );
        } );
 //   } );
        </script>