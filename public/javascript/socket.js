// Connect to the nodeJs Server
//socketServer = io.connect( '/' );

// (1): Send a ping event with
// some data to the server
//console.log( "Browser says ping to server (1)" );
//socketServer.emit('ping', { some: 'data' } );
//
//// (4): When the browser recieves a pong event
//// console log a message and the events data
//socketServer.on('pong', function (data) {
//  console.log( 'Browser recieves pong from server (4)', data );
//});
